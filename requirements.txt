# Direct dependencies, pinned to the versions the results in results/ were
# produced with (Python 3.9).
#
# vamm is not on PyPI and is not installed from here; it ships as a copy in
# external/ and needs a C++ build:  pip install ./external/vamm

numpy==2.0.2
scipy==1.13.1
matplotlib==3.9.4
dearpygui==2.3.1        # the demo GUI
gudhi==3.11.0           # persistent homology
torchvision==0.23.0     # only to download MNIST (pulls in torch)
